# Keep the build context small and OS-clean. netcoredbg is fetched fresh (linux)
# during publish, so don't ship the local (Windows) cache or build artifacts.
**/bin/
**/obj/
tools/netcoredbg/
spike/linux/.netcoredbg/
spike/linux/.server/
coverage-report/
**/TestResults/
.git/
.vs/
.github/
